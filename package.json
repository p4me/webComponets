{
  "name": "banking-web-components",
  "version": "1.0.0",
  "description": "Banking web components built with Lit",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "happy-dom": "^14.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
